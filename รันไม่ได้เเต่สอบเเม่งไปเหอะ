#include<stdio.h>
int main(){
	int n,mid,final,work,garde;
	int sum,avg,avh1;
	printf("\n studen num : ");
	scanf("%d",&n);
	char fname[n][50];
	char surname[n][50];
	float heignt[n];
	float weignt[n];
	float gpas[n];
	for(i=0 , i < n ,i++){
		sum=0
		printf("name : ");
		scanf("%d",fname);
		printf("surname : ");
		scanf("%d",surname);
		printf("heignt");
		scanf("%f",heignt);
		printf("weignt : ");
		scanf("%d",weignt);
		for (j=0, j < 5 , J++){
			printf("subjec %d",j+1);
			printf("midscore : ");
			scanf("%d",&mid);
			printf("finalscore : ");
			scanf("%d",&final);
			printf("assscore : ");
			scanf("%d",work);
			sum=mid+final+ass
			
			if(sum>80){
				grade=4;
			}else if(sum>70){
				grade=3;
			}else if(sum>60){
				grade=2
			}else if(sum>50){
				grade=1
			}else{
				grade=0
			}
			avg=avg+grade;
			}
			avg=avg/5;
			printf("\navg=%.2f",avg1);
			gpas[i]=avg1;
		}
		return(0);

	}
